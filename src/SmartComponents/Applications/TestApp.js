import React from 'react';
import { Main } from '@redhat-cloud-services/frontend-components';

const TestApp = () => {
    return (
        <React.Fragment>
            <Main>
                <h1>Test App Settings</h1>
                <p> The is the route to the test app settings page </p>
            </Main>
        </React.Fragment>
    );
};

export default TestApp;
